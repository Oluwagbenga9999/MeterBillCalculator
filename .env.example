# Vercel ↔ Supabase Marketplace defaults (also work locally).
# https://supabase.com/docs/guides/integrations/vercel-marketplace

NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key-here

# Server-only secret key (never expose to the browser)
SUPABASE_SECRET_KEY=your-secret-key-here

# Database — auto-applied on server start (instrumentation) and via npm run db:migrate.
# Vercel ↔ Supabase also sets POSTGRES_URL / POSTGRES_URL_NON_POOLING.
POSTGRES_PASSWORD=your-database-password
# Recommended for Vercel migrations: Supabase Session Pooler URL (IPv4, port 5432).
# This takes priority over the integration-provided URLs.
# POSTGRES_MIGRATION_URL=postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres
# Optional full connection string (preferred when present):
# POSTGRES_URL=postgresql://postgres:PASSWORD@db.PROJECT_REF.supabase.co:5432/postgres
# POSTGRES_URL_NON_POOLING=...
# SKIP_DB_MIGRATE=1

# App-specific (not set by Supabase integration)
SUPERADMIN_EMAIL=superadmin@example.com
SUPERADMIN_PASSWORD=change-me-strong-password
NEXT_PUBLIC_SITE_URL=http://localhost:3000
